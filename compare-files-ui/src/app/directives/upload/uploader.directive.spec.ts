import { UploaderDirective } from './uploader.directive';

describe('UploaderDirective', () => {
  it('should create an instance', () => {
    const directive = new UploaderDirective();
    expect(directive).toBeTruthy();
  });
});
